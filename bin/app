#!/usr/bin/env php
<?php

require_once __DIR__ . '/../vendor/autoload.php';

use Symfony\Component\Console\Application;


define('APP_NAME', 'Illuminate CLI App');
define('APP_VERSION', '0.0.1');

$app = new Application(APP_NAME, APP_VERSION);

$app->run();
